---
title: Getting Started with Vue
description: Learn how to integrate VistaView with Vue 3 applications
---

VistaView provides official Vue 3 bindings with both a declarative component and a composable.

## Installation

import { Tabs, TabItem } from '@astrojs/starlight/components';

<Tabs syncKey="pkg">
  <TabItem label="npm">

  ```bash
  npm install vistaview
  ```

  </TabItem>
  <TabItem label="yarn">

  ```bash
  yarn add vistaview
  ```

  </TabItem>
  <TabItem label="pnpm">

  ```bash
  pnpm add vistaview
  ```

  </TabItem>
  <TabItem label="bun">

  ```bash
  bun add vistaview
  ```

  </TabItem>
</Tabs>

## Component Approach (Recommended)

The `VistaView` component provides a declarative way to create image galleries:

```vue
<script setup>
import { VistaView } from 'vistaview/vue';
import 'vistaview/style.css';
</script>

<template>
  <VistaView selector="> a">
    <a href="/images/photo1-full.jpg">
      <img src="/images/photo1-thumb.jpg" alt="Photo 1" />
    </a>
    <a href="/images/photo2-full.jpg">
      <img src="/images/photo2-thumb.jpg" alt="Photo 2" />
    </a>
  </VistaView>
</template>
```

### With Ref for Imperative Control

We expose both the imperative API and the root DOM element on the component ref. Use a typed component ref to access `vista` (API) and `container` (DOM):

```vue
<script setup lang="ts">
import { ref } from 'vue';
import type { VistaInterface } from 'vistaview';
import { VistaView } from 'vistaview/vue';
import 'vistaview/style.css';

const compRef = ref<{ vista: VistaInterface; container: HTMLElement | null } | null>(null);

const openGallery = () => {
  compRef.value?.vista.open(0);
  console.log(compRef.value?.container);
};
</script>

<template>
  <VistaView ref="compRef" selector="> a">
    <a href="/images/full.jpg">
      <img src="/images/thumb.jpg" alt="Photo" />
    </a>
  </VistaView>
  <button @click="openGallery">Open Gallery</button>
</template>
```

> Note: `compRef.value.vista` gives the API and `compRef.value.container` gives the root DOM element.

### With Options

```vue
<script setup>
import { VistaView } from 'vistaview/vue';
import type { VistaOpt } from 'vistaview';
import 'vistaview/style.css';

// Define options outside component to prevent recreation on every render
const options: VistaOpt = {
  maxZoomLevel: 3,
  preloads: 2,
  animationDurationBase: 400,
  onOpen: (vistaView) => console.log('Gallery opened'),
  onClose: (vistaView) => console.log('Gallery closed'),
};
</script>

<template>
  <!-- selector defaults to '> a' -->
  <VistaView :options="options">
    <a href="/images/photo1-full.jpg">
      <img src="/images/photo1-thumb.jpg" alt="Photo 1" />
    </a>
    <a href="/images/photo2-full.jpg">
      <img src="/images/photo2-thumb.jpg" alt="Photo 2" />
    </a>
  </VistaView>
</template>
```

## Composable Approach

Use the `useVistaView` composable for more control:

```vue
<script setup>
import { useId } from 'vue';
import { useVistaView } from 'vistaview/vue';
import 'vistaview/style.css';

const id = useId();
const vista = useVistaView({
  elements: `#${CSS.escape(id)} a`,
});
</script>

<template>
  <div :id="id">
    <a href="/images/full.jpg">
      <img src="/images/thumb.jpg" alt="Photo" />
    </a>
  </div>
  <button @click="vista.open(0)">Open Gallery</button>
  <button @click="vista.next()">Next</button>
  <button @click="vista.prev()">Previous</button>
</template>
```

## With Extensions

```vue
<script setup>
import { useVistaView } from 'vistaview/vue';
import { download } from 'vistaview/extensions/download';
import 'vistaview/style.css';

const vista = useVistaView({
  elements: '#gallery a',
  controls: {
    topRight: ['zoomIn', 'zoomOut', 'download', 'close'],
  },
  extensions: [download()],
});
</script>

<template>
  <div id="gallery">
    <a href="/images/photo1.jpg">
      <img src="/images/photo1-thumb.jpg" alt="Photo 1" />
    </a>
  </div>
</template>
```

## Options API

If you prefer the Options API:

```vue
<script>
import { vistaView } from 'vistaview';
import 'vistaview/style.css';

export default {
  data() {
    return {
      vista: null,
    };
  },
  mounted() {
    this.vista = vistaView({
      elements: '#gallery a',
    });
  },
  beforeUnmount() {
    this.vista?.destroy();
  },
};
</script>

<template>
  <div id="gallery">
    <a href="/images/photo1.jpg">
      <img src="/images/photo1-thumb.jpg" alt="Photo 1" />
    </a>
  </div>
</template>
```

## Next Steps

- Explore [configuration options](/core/configuration/complete)
- Learn about [extensions](/extensions/overview)
- Customize the [styling](/styling/themes)
