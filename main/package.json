{
  "name": "main",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite build --watch\" \"tsc --watch\"",
    "build": "UMD=1 vite build && vite build && tsc",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/trusted-types": "^2.0.7",
    "concurrently": "^8.2.2",
    "typescript": "~5.9.3",
    "vite": "npm:rolldown-vite@7.2.5"
  }
}
