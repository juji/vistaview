import type { VistaData } from '../types';
import type { VistaView } from '../vista-view';

// run everytime new set of images are to be viewed
export function imageSetup(_params: VistaData, _vistaView: VistaView) {
  // deliberately empty
}
